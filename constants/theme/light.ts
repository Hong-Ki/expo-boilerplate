import { createColorSet } from ".";

export const light = {
  colorSet: createColorSet({
    background: "#FFFFFF",
    backgroundSub: "#DBDBDB",
    text: "#111111",
    textSub: "#080826",
  }),
};

export default light;
