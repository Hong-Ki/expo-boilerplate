import { createColorSet } from ".";

const dark = {
  colorSet: createColorSet({
    background: "#111111",
    backgroundSub: "#080826",
    text: "#FFFFFF",
    textSub: "#DBDBDB",
  }),
};

export default dark;
